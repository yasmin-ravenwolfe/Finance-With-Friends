require "rails_helper"

RSpec.describe Group do
  describe '#group_report' do
  context "with a specified date range, all members, all categories" do
    it "returns total each member spent from receipts in date range" do

    end
  end
end
